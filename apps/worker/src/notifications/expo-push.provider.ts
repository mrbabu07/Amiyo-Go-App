export type PushMessage = { body: string; data: Record<string, unknown>; title: string; to: string };
export interface PushProvider { send(messages: PushMessage[]): Promise<void>; }
export class ExpoPushProvider implements PushProvider { async send(messages: PushMessage[]) { if (!messages.length) return; const response = await fetch("https://exp.host/--/api/v2/push/send", { method: "POST", headers: { Accept: "application/json", "Accept-Encoding": "gzip, deflate", "Content-Type": "application/json" }, body: JSON.stringify(messages) }); if (!response.ok) throw new Error(`Expo Push request failed (${response.status})`); const payload = await response.json() as { data?: Array<{ status?: string; message?: string }> }; const error = payload.data?.find((item) => item.status === "error"); if (error) throw new Error(error.message || "Expo Push rejected a notification"); } }
