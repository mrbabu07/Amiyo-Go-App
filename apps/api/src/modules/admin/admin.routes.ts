import { Router } from "express"; import { rateLimit } from "express-rate-limit"; import { z } from "zod";
import { adminAnalyticsQuerySchema, adminBannerInputSchema, adminCategoryAttributesInputSchema, adminCategoryInputSchema, adminCategoryRequestReviewSchema, adminKycReviewInputSchema, adminToggleInputSchema, adminUserRolesInputSchema, adminUserStatusInputSchema, adminVendorStatusInputSchema, paymentVerificationReviewSchema, trustCaseActionInputSchema } from "@amiyo/contracts";
import { prisma } from "../../infrastructure/database/prisma.js"; import { FirebaseTokenVerifier } from "../identity/firebase-token.verifier.js"; import { createAuthenticationMiddleware, requireSession } from "../identity/identity.middleware.js"; import { IdentityService } from "../identity/identity.service.js"; import { AdminService } from "./admin.service.js";
const idSchema = z.object({ id: z.string().uuid() });
export function createAdminRouter() { const router = Router(); const service = new AdminService(prisma); const authenticate = createAuthenticationMiddleware(new FirebaseTokenVerifier(), new IdentityService(prisma)); const writes = rateLimit({ windowMs: 60_000, limit: 30, standardHeaders: "draft-7", legacyHeaders: false, message: { title: "Too many admin requests", status: 429, code: "ADMIN_RATE_LIMITED" } }); router.use("/api/v2/admin/workspace", authenticate); router.get("/api/v2/admin/workspace", async (req, res, next) => { try { res.json(await service.workspace(requireSession(req))); } catch (error) { next(error); } }); router.patch("/api/v2/admin/workspace/users/:id/status", writes, async (req, res, next) => { try { res.json(await service.updateUser(requireSession(req), idSchema.parse(req.params).id, adminUserStatusInputSchema.parse(req.body))); } catch (error) { next(error); } }); router.put("/api/v2/admin/workspace/users/:id/roles", writes, async (req, res, next) => { try { res.json(await service.updateUserRoles(requireSession(req), idSchema.parse(req.params).id, adminUserRolesInputSchema.parse(req.body))); } catch (error) { next(error); } }); router.patch("/api/v2/admin/workspace/vendors/:id/status", writes, async (req, res, next) => { try { res.json(await service.updateVendor(requireSession(req), idSchema.parse(req.params).id, adminVendorStatusInputSchema.parse(req.body))); } catch (error) { next(error); } }); router.patch("/api/v2/admin/workspace/kyc/:id", writes, async (req, res, next) => { try { res.json(await service.reviewKyc(requireSession(req), idSchema.parse(req.params).id, adminKycReviewInputSchema.parse(req.body))); } catch (error) { next(error); } }); router.post("/api/v2/admin/workspace/trust-cases/:id/actions", writes, async (req, res, next) => { try { res.json(await service.actOnTrustCase(requireSession(req), idSchema.parse(req.params).id, trustCaseActionInputSchema.parse(req.body))); } catch (error) { next(error); } }); router.get("/api/v2/admin/workspace/platform", async (req, res, next) => { try { res.json(await service.platform(requireSession(req))); } catch (error) { next(error); } }); router.get("/api/v2/admin/workspace/analytics", async (req, res, next) => { try { res.json(await service.analytics(requireSession(req), adminAnalyticsQuerySchema.parse(req.query))); } catch (error) { next(error); } }); router.get("/api/v2/admin/workspace/category-requests", async (req, res, next) => { try { res.json(await service.categoryRequests(requireSession(req))); } catch (error) { next(error); } }); router.patch("/api/v2/admin/workspace/category-requests/:id", writes, async (req, res, next) => { try { res.json(await service.reviewCategoryRequest(requireSession(req), idSchema.parse(req.params).id, adminCategoryRequestReviewSchema.parse(req.body))); } catch (error) { next(error); } }); router.patch("/api/v2/admin/workspace/payment-verifications/:id", writes, async (req, res, next) => { try { res.json(await service.reviewPayment(requireSession(req), idSchema.parse(req.params).id, paymentVerificationReviewSchema.parse(req.body))); } catch (error) { next(error); } }); router.post("/api/v2/admin/workspace/categories", writes, async (req, res, next) => { try { res.status(201).json(await service.createCategory(requireSession(req), adminCategoryInputSchema.parse(req.body))); } catch (error) { next(error); } }); router.put("/api/v2/admin/workspace/categories/:id/attributes", writes, async (req, res, next) => { try { res.json(await service.replaceCategoryAttributes(requireSession(req), idSchema.parse(req.params).id, adminCategoryAttributesInputSchema.parse(req.body))); } catch (error) { next(error); } }); router.post("/api/v2/admin/workspace/banners", writes, async (req, res, next) => { try { res.status(201).json(await service.createBanner(requireSession(req), adminBannerInputSchema.parse(req.body))); } catch (error) { next(error); } }); router.put("/api/v2/admin/workspace/banners/:id", writes, async (req, res, next) => { try { res.json(await service.updateBanner(requireSession(req), idSchema.parse(req.params).id, adminBannerInputSchema.parse(req.body))); } catch (error) { next(error); } }); router.delete("/api/v2/admin/workspace/banners/:id", writes, async (req, res, next) => { try { res.json(await service.deleteBanner(requireSession(req), idSchema.parse(req.params).id)); } catch (error) { next(error); } }); router.patch("/api/v2/admin/workspace/banners/:id", writes, async (req, res, next) => { try { res.json(await service.toggleBanner(requireSession(req), idSchema.parse(req.params).id, adminToggleInputSchema.parse(req.body).active)); } catch (error) { next(error); } }); router.patch("/api/v2/admin/workspace/vouchers/:id", writes, async (req, res, next) => { try { res.json(await service.toggleVoucher(requireSession(req), idSchema.parse(req.params).id, adminToggleInputSchema.parse(req.body).active)); } catch (error) { next(error); } }); router.patch("/api/v2/admin/workspace/flash-sales/:id", writes, async (req, res, next) => { try { res.json(await service.toggleFlashSale(requireSession(req), idSchema.parse(req.params).id, adminToggleInputSchema.parse(req.body).active)); } catch (error) { next(error); } }); return router; }
